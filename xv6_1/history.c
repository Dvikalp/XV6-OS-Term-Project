#include "types.h"
#include "user.h"
#include "stat.h"
#include "param.h"

#define MAX_CMD 128
#define MAX_HISTORY 16

int main(int argc, char *argv[])
{
  // char buffer[MAX_CMD]; // Buffer to hold the history command
  // int id;

  // printf(1, "History:\n"); // Print header

  // for(id = 0; id < MAX_HISTORY; id++){ // Loop through all possible ids
  //   if(history(buffer, id) == 0){ // If history succeeds
  //     printf(1, "%d: %s\n", id, buffer); // Print history command with id 
  //   }
  // }

  exit(); // Exit the program
}